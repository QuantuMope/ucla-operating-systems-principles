lab3b - Shell script that runs a python program conducting a file
        system consistency analysis. Processes csv files containing
        summaries of all the data structures within an EXT2 file system
        and finds and reports anomalies. Checks include block consistency
        audits, i-node allocation audits, and directory consistency
        audits.

-------------------------------------------------------------------
Included files:
lab3b.py  - Python source code
README    - description of all included files
Makefile  - makefile to build program and tarball, supports
            the following commands:
            make         - build the lab3b shell script that runs the
                           python source code
            make clean   - remove any files created by the Makefile
            make dist    - build a tarball lab3b-205348339.tar.gz
                           containing all included files
